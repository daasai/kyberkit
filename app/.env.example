# Kevin web app (Vite) — copy to `app/.env` or set in shell when running `npm run dev`
# Base URL of the Bun Sidecar (see repo root `bun src-sidecar/index.ts`, default port 3001)
VITE_SIDECAR_URL=http://localhost:3001
